{
  "name": "style-cookies",
  "version": "0.0.1",
  "description": "Using cookies to tell the server not to deliver inline styles anymore.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cookie-parser": "^1.4.4",
    "express": "^4.17.0"
  }
}
